{% extends 'base.html.twig' %}

{% block title %}Éditer la catégorie{% endblock %}

{% block body %}
    <h1>Éditer la catégorie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-success">Sauvegarder</button>
    {{ form_end(form) }}

    <a href="{{ path('category_index') }}" class="btn btn-danger">Retour à la liste</a>
{% endblock %}